/** Cells on one side of a Bindog bingo board. */
export const BINGO_BOARD_SIZE = 5 as const

/** Total cells on a 5×5 board. */
export const BINGO_CELL_COUNT = 25 as const

/** Non-wild breeds dealt onto each board. */
export const BINGO_BREEDS_PER_BOARD = 24 as const
