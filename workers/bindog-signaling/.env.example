# Local secrets for `wrangler dev` (not used by deployed Worker).
# Copy to `.env` and fill in values from Cloudflare Realtime TURN.
TURN_KEY_ID=
TURN_API_TOKEN=

# Dev-only origins (overrides wrangler.toml [vars] during `wrangler dev`).
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
